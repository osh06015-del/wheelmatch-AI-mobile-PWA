# 9월 30일까지 로드맵

갱신 2026-09-01. 지금까지 논의된 기능을 전부 모아 실현 가능성과 순서를 정리한다.

앱의 정체성은 이것으로 잡는다.

> 작업자가 그라인더를 쓰기 직전, 숫돌 선택과 기본 안전확인을 30초 안에 끝내면서
> 위험한 조합은 자동으로 차단하는 모바일 사전점검 도구.

---

## 지금까지 된 것

앱은 배포되어 실제로 동작한다. <https://wheelmatch-nu.vercel.app>

| 기능                                                 | 상태 |
| ---------------------------------------------------- | ---- |
| 작업 목적 선택 (절단/연삭) → 명판 → 숫돌 → 판정      | ✅   |
| RPM·지름·용도 자동 대조 + 판정불가 Fail-safe         | ✅   |
| **작업 목적 ↔ 숫돌 용도 대조** (불일치 시 부적합)    | ✅   |
| **숫돌 종류 판별** (다이아몬드·플랩·컵휠 → 판정불가) | ✅   |
| **외관 손상 경고** (경고 방향으로만)                 | ✅   |
| **즉시 행동지침** (차이값 + 조치 문장)               | ✅   |
| **항목별 작업자 설명** (뭔지 + 어디 적혀있는지)      | ✅   |
| 안전 체크리스트 4개 + 작업 직전 안내                 | ✅   |
| 갤러리 업로드 (촬영과 동등)                          | ✅   |
| 점검 이력 저장 (IndexedDB)                           | ✅   |
| 이미지 자동 축소 (Vercel 4.5MB 한도)                 | ✅   |
| 카톡 링크 미리보기 카드                              | ✅   |
| CI (push마다 자동 검증)                              | ✅   |

규칙엔진 검사 8종, 테스트 114개.

---

## 실현 가능성 판정

논의된 기능을 전부 모아 판정한다. **불가능한 것은 하나뿐이다.**

### ✅ 가능하고 값싼 것 (각 1~2시간)

| 기능                            | 판정          | 비고                                                                       |
| ------------------------------- | ------------- | -------------------------------------------------------------------------- |
| 그라인더 크기·규격 표시         | 가능          | 명판에서 이미 읽고 있다. 인치 등급(Φ100=4인치, Φ125=5인치)만 덧붙이면 된다 |
| **필요 숫돌 조건 안내**         | 가능          | 명판 값을 뒤집으면 바로 나온다. 아래 주의 참고                             |
| 안전 여유율 % 자동 계산         | 가능          | `(숫돌정격 − 그라인더속도) / 그라인더속도 × 100`                           |
| 소요시간 기록                   | 가능          | 시작~저장 경과시간을 기록에 함께 저장                                      |
| 저장된 사진을 이력에 표시       | 가능          | 이미 저장 중인데 보여주지 않는다. 표시만 하면 된다                         |
| 이력을 작업별로 구분·필터       | 가능          | `declaredPurpose`가 이미 저장된다                                          |
| 규칙 버전 표시                  | 가능          | 단, **기준 문서명은 직접 확인 필요**                                       |
| 체크리스트/규격판정 시각적 분리 | 가능          | 이미 분리되어 있고 강조만 더하면 된다                                      |
| CSV 내보내기                    | 가능          | 논문 표에 바로 쓴다                                                        |
| QR 코드                         | **검증 완료** | 오류정정 **H** 필수. 아래 별도 항목 참고                                   |

### ✅ 가능하되 품이 드는 것

| 기능                          | 시간 | 비고                                                         |
| ----------------------------- | ---- | ------------------------------------------------------------ |
| **다국어 5개 언어**           | 6~8h | 한국어·영어·베트남어·인도네시아어·중국어. **아래 주의 참고** |
| 물리 교차검증 (원주속도)      | 3h   | 정상 55~80 m/s, 자릿수 오인식은 8 / 798 m/s로 튄다           |
| 작업별 위험사항 나열          | 2h   | 고정 문구. AI 자유생성 금지                                  |
| 연구·실험 모드 (Ground Truth) | 6h   | False-Safe Rate 등 논문 지표가 자동으로 나온다               |
| OCR 이중검증                  | 4h   | **아래 주의 참고**                                           |
| 촬영 품질 게이트              | 5h   | 차단이 아니라 경고로. 임계값은 실측 후 보정                  |
| 판정 근거 표시                | 3h   | **crop은 불가**. 아래 주의 참고                              |
| 데모 모드 (오프라인 시연)     | 2h   | 발표장 네트워크 장애 보험                                    |
| 하이브리드 OCR                | 4h   | 깨끗한 라벨은 무료·오프라인 처리                             |
| 이미지 전처리                 | 4h   | 기울기 보정·이진화. 인식률 개선                              |

### ⚠ 설계를 바꿔야 하는 것 (4건)

**1. "어떤 숫돌을 끼워야 할지" — 추천이 아니라 조건으로**

제품 추천은 제안서가 명시적으로 제외한 **'안전 승인'** 에 다가선다.
대신 명판에서 곧바로 따라 나오는 조건만 적는다.

```
연삭 작업 · GWS 750-125 (11,000rpm, 최대 Φ125mm)
→ 필요한 숫돌 조건
   연삭용 / Φ125mm 이하 / 최고사용회전속도 11,000rpm 이상
```

규칙엔진의 판정 기준을 그대로 뒤집은 것이라 둘이 어긋날 수 없다.
숫돌을 고르기 **전에** 무엇을 봐야 하는지 알려주므로 현장에서 더 유용하다.

**2. 판정 근거 표시 — crop은 불가능**

Claude는 글자의 **좌표를 돌려주지 않는다**. 모델에게 좌표를 물으면 정밀도가
낮아 엉뚱한 영역을 잘라 보여준다. 근거로 만든 화면이 틀린 근거를 보여주면
없느니만 못하다.

대신 `전체 사진 + 읽어낸 원문 + 값 + 출처`로 간다. 추적성은 그대로 성립한다.
진짜 crop이 필요하면 Tesseract 경로에서만 가능하다(bbox를 준다).

**3. OCR 이중검증 — 기권과 불일치를 구분**

실측(`docs/offline-ocr.md`)에서 현장 사진의 Tesseract 결과는 이랬다.

|           | 결과                                      |
| --------- | ----------------------------------------- |
| Claude    | `GWS 750-125` / `11000` / `125` 전부 정확 |
| Tesseract | `BO 0000` / null / null                   |

"불일치하면 차단"을 그대로 넣으면 **현장 사진은 거의 전부 판정불가**가 된다.

| Tesseract    | 처리                         |
| ------------ | ---------------------------- |
| 값 없음      | **기권** — 불일치 아님, 통과 |
| 값 있고 다름 | **불일치** → 판정불가        |
| 값 있고 같음 | 일치                         |

이러면 "두 경로가 충돌하면 판단을 거부한다"는 서사는 살면서 앱이 쓸모를 잃지 않는다.

**4. "오차율" — 두 가지를 구분해야 한다**

| 무엇의 오차인가                              | 필요한 것               | 어디에 쓰나 |
| -------------------------------------------- | ----------------------- | ----------- |
| **안전 여유율** (숫돌 정격 vs 그라인더 속도) | 없음. 지금 값으로 계산  | 작업자 화면 |
| **OCR 정확도 오차율** (읽은 값 vs 실제 값)   | **정답 입력(연구모드)** | 논문        |

앞의 것은 바로 되고, 뒤의 것은 연구모드가 있어야 한다. 둘 다 넣되 화면을 나눈다.
작업자에게 OCR 정확도 통계는 필요 없다.

### ❌ 넣지 않는 것

| 기능                            | 이유                                                         |
| ------------------------------- | ------------------------------------------------------------ |
| AI가 "안전합니다" 승인          | 제안서 방어전략이 무너진다. 금지선                           |
| 사진으로 균열 "없음" 판정       | 미세균열은 사진에 안 보인다. 표준은 타음검사                 |
| 체크리스트 자동 체크            | 실험했다. AI가 안전 조건을 단언하게 되어 방향이 반대         |
| 사진으로 그라인더 **치수 측정** | 기준물 없이 사진에서 크기를 재면 오차가 크다. 명판 값을 쓴다 |
| 바코드 스캔                     | SKU를 읽어도 규격 DB가 없다                                  |
| 로그인 / 서버 DB                | 기기 안 저장이 이 앱의 설계다                                |

---

## 다국어 — 반드시 짚을 것

제안서 기대효과에 "외국인 작업자 지원"이 있고, 현재 UI 문자열은 전부 한국어다.
넣는 것은 기술적으로 어렵지 않다.

**문제는 번역 품질이다.**

이 앱의 문장 상당수가 안전 문구다.

> "숫돌 최고사용회전속도가 그라인더 무부하 회전속도보다 낮습니다. 파손·비산 위험이 있습니다."

**안전 문구를 검수 없이 기계 번역으로 내보내면 안 된다.** 잘못 번역된 경고는
경고가 없는 것보다 나쁠 수 있다.

권장 방식:

1. 구조를 먼저 만든다 (문자열 분리 + 언어 전환)
2. **판정 3색(적합/부적합/판정불가)과 행동지침만 우선 번역** — 가장 중요한 문장
3. 그 문장들만 해당 언어 사용자에게 검수받는다
4. 나머지 설명문은 그 다음에

색과 아이콘은 언어와 무관하게 읽히므로, 글자를 못 읽어도 최소한의 정보는 전달된다.
이것이 이 앱이 다국어에서 유리한 점이다.

---

## QR 코드 — 검증 완료

실제로 QR을 만들어 인쇄·촬영 조건을 흉내 내 확인했다.

|                                  |                                               |
| -------------------------------- | --------------------------------------------- |
| URL                              | 32자, QR 버전 3 (29×29 모듈) — 매우 낮은 밀도 |
| 크기별                           | 80px~400px 전부 디코딩 성공                   |
| 기울임 15°·30° / 저조도 / 노이즈 | 전부 성공                                     |
| **초점 흐림(심함)**              | **실패**                                      |

**오류정정 레벨을 H로 할 것.** 대부분의 QR 생성기 기본값은 M이다.

| 레벨  | 견디는 오염 |
| ----- | ----------- |
| L / M | 5%          |
| Q     | 10%         |
| **H** | **15%**     |

**스캔 후 열리는 브라우저가 더 중요하다.** 카카오톡·네이버 등의 인앱 브라우저는
카메라를 막는 경우가 많다. 다행히 갤러리 업로드 경로가 있어 앱이 죽지는 않지만,
현장에서 바로 찍는 흐름은 막힌다. QR 옆에 이 문구를 함께 인쇄한다.

> 스캔 후 **"기본 브라우저로 열기"** 를 눌러주세요. (카메라 사용)

---

## 아직 0%인 것 — 제안서가 약속한 검증

여기가 가장 큰 구멍이다. **코드로 채울 수 없다.**

| 제안서 요구                  | 필요한 것        |
| ---------------------------- | ---------------- |
| 기계·숫돌 **20세트** 실측    | 실물 장비 + 촬영 |
| 수기 vs 앱 **소요시간 비교** | 사람이 초시계로  |
| **5~10명 SUS 설문**          | 대상자 섭외      |

주의: 내가 만든 "규칙엔진 테스트 20개"는 제안서의 "20세트"와 **다른 것**이다.
전자는 로직 검증(합성 값), 후자는 실물 촬영 실험이다. 발표에서 섞어 말하면 지적당한다.

**실험 설계 요점:** 판정이 결정론적 규칙이므로 **부적합 탐지 재현율 = 필드 추출 정확도**다.
추출만 맞으면 판정은 항상 맞는다. 재야 할 것은 하나뿐이고,
"판정 오류"와 "인식 오류"를 분리해 보고할 수 있다.

---

## 주차별 계획

### 1주차 (9/1~9/7) — 현장 기능 + 측정 준비

- [x] 필요 숫돌 조건 안내 — `lib/rules/requirement.ts`, `RequirementBanner`
- [x] 그라인더 규격·인치 등급 표시 — `grinderSummary()`
- [x] 안전 여유율 % — 결과 화면 규칙별로 표시
- [x] 소요시간 기록 — 작업 선택~저장. `lib/record/elapsed.ts`
- [x] 저장 사진 이력 표시 + 작업별 구분 — `HistoryList`
- [x] 작업별 위험사항 나열 — `lib/guide/hazards.ts`, `HazardList`
- [x] **연구·실험 모드 + CSV** — `lib/record/csv.ts`, `ResearchPanel`.
      OCR 원본값을 함께 저장하도록 바꿔 인식률·정정률을 잴 수 있다
- [ ] **장비 섭외 시작** ← 가장 오래 걸린다 (사용자 몫)

1주차 코드 작업은 2026-09-01에 끝났다. 테스트 178개 통과.
남은 것은 실물 장비 섭외다. 그것이 없으면 2주차 실측을 시작할 수 없다.

### 2주차 (9/8~9/14) — 실측 + 다국어 구조

- **실물 20세트 촬영·측정**
- 다국어 구조 + 판정·행동지침 우선 번역 (6h)
- 물리 교차검증 (3h)

### 3주차 (9/15~9/21) — 사용성 + 발표 준비

- SUS 설문 5~10명
- 데모 모드 (2h)
- 번역 검수
- QR 인쇄물 제작·현장 테스트

### 4주차 (9/22~9/30) — 논문·슬라이드

- **9/26 코드 동결** — 이후 버그 수정만
- 실기기 최종 점검, 리허설

여유가 생기면 OCR 이중검증, 촬영 품질 게이트, 하이브리드 OCR, 이미지 전처리.
**이 넷은 없어도 발표가 된다.**

---

## 시간 합계

| 구분                      | 시간     | 발표 필수                    |
| ------------------------- | -------- | ---------------------------- |
| 1주차 현장 기능           | ~9h      | ✅                           |
| 연구모드 + CSV            | ~8h      | ✅ 논문 숫자가 여기서 나온다 |
| 다국어 (구조 + 핵심 번역) | ~6h      | ✅ 제안서 주장               |
| 물리 교차검증             | ~3h      | ✅                           |
| 데모 모드                 | ~2h      | ✅ 시연 보험                 |
| **소계**                  | **~28h** |                              |
| 선택 4종                  | ~16h     | ⬜                           |

**필수만 약 28시간.** 4주 안에 가능하다.
가장 오래 걸리는 것은 코드가 아니라 **실물 20세트 촬영과 설문**이다.

---

## 착수 전 확인이 필요한 것

- **80 m/s 상한** — 상용 통례로 계산한 값이다. 안전 규칙으로 코드에 넣기 전
  KS/EN 12413 원문을 확인하고 근거를 주석에 남긴다.
- **규칙 버전에 적을 기준 문서명** — 실제 KOSHA 지침 번호를 확인해야 한다.
  추측한 문서명을 적으면 안 된다.
- **불꽃 방향을 체크리스트에서 뺀 것** — 제안서에는 5개로 적혀 있다.
  현재는 작업 직전 안내로 옮겨두었다. 이대로 갈지 되돌릴지 정해야 한다.
